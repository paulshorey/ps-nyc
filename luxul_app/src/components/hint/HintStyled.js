import styled from 'styled-components';

export const Hint = styled.div`
	display: inline-block;
	sup {
		position: relative;
		top: -0.1rem;
		display: inline-block;
		font-size: 0.9rem;
		opacity: 0.5;
		cursor: pointer;
	}
`;
